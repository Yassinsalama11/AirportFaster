{
  "framework": "nextjs",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter @airportfaster/web build",
  "outputDirectory": ".next",
  "env": {
    "SKIP_ENV_VALIDATION": "1"
  }
}
